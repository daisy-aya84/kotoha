<%= form_tag("/post/create") do%>
    <textarea name="content"></textarea>
    <input type="submit" value="投稿する">
<% end %>

<% @posts.each do |post| %>
    <%= link_to(post.user.name, "/user/#{post.user.id}/show") %>
    <%= link_to(post.content, "/post/#{post.id}") %>
<% end %>